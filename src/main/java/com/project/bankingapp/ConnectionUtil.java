package com.project.bankingapp;

import java.sql.DriverManager;
import java.util.Properties;

public class ConnectionUtil {
	//needs a url
	//needs a username
	//needs a password
	/*InputStream in = null;
	try {
		Properties props = new Properties();
		in = new FileInputStream("")props.load(in);
		
		Class.forName("oracle.jdbs.driver.OracleDriver");
		connection con = null;
		
		String endpoint = props.getProperty("jdbs.url");
		String username = props.getProperty("jdbs.username");
		String password = props.getProperty("jdbs.password");
		
		con = DriverManager.getConnections(endpoint, username, password);
		connectionInstance = con;
		return connectionInstance;
	}catch (Exception e) {
		
	}*/
}
